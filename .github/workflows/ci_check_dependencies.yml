name: CI - Check dependencies

on:
  schedule:
    - cron: "30 5 * * 1"
  workflow_dispatch:

jobs:
  check-dependencies:
    name: External
    uses: SINTEF/ci-cd/.github/workflows/ci_check_pyproject_dependencies.yml@cwa/debug-ci-check-dependencies
    if: github.repository_owner == 'Materials-Consortia'
    with:
      git_username: OPTIMADE Developers
      git_email: "dev@optimade.org"
      permanent_dependencies_branch: cwa/test-ci-workflow
      python_version: "3.8"
      install_extras: "[dev]"
      pr_labels: "dependencies"
    secrets:
      PAT: ${{ secrets.RELEASE_PAT_CASPER }}
